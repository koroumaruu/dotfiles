#!/bin/sh
#honestly i oughta delete this 
# swaybg -i ~/Downloads/blackhole2.jpg >/dev/null 2>&1 & 
# waybar -c ~/.config/waybar/config.jsonc -s ~/.config/waybar/style.css >/dev/null 2>&1 &
# dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=wlroots
# The next line of command is not necessary. It is only to avoid some situations where it cannot start automatically
#k /usr/lib/xdg-desktop-portal-wlr &
